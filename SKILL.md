---
name: tshock-plugin-dev
description: TShock 插件全流程开发：需求采集、版本动态解析、参考源码获取、分层TDD编码、代码审查、编译、部署与服务器加载验证。当用户要求开发或修改 Terraria TShock 插件（Terraria 1.4.5+ / TShock 6.0.0+）时调用。
---

# TShock 插件开发（tshock-plugin-dev）

面向 Terraria 1.4.5 及以后任何版本的 TShock 插件开发技能。兼顾高级开发者（本地版本化源码）与小白（零本地环境，全自动准备）。

## 何时使用

- 用户要求开发一个全新的 TShock 插件
- 用户要求修改/修复/扩展已有 TShock 插件
- 用户提到 Terraria / TShock / ServerPlugins / TerrariaPlugin 等关键词的代码任务

## 工作流（十阶段）

严格按阶段顺序执行。每个阶段结束向用户简报进展。

### Phase 0 需求采集（必答）

按 `references/00-需求采集.md` 提问。**必须先提问、不直接自动探测**（参照 brainstorming 一问一答风格，一次一问、给选项）：

1. **目标服务端版本**：问用户「TShock 版本号 / 提供服务器文件夹 / 用最新 / 不清楚」——只有用户明确无法回答时才进入 Phase 1 探测，探测结果回显确认
2. **目标客户端版本**：问用户 Terraria 版本（不确定可选「与服务器一致」或「最新」）
3. **其他参考源**：主动问「是否有其他可参考的 GitHub 仓库或本地源码？」

**版本门禁（硬性）**：若用户目标的 Terraria 版本没有对应 TShock NuGet 包（TShock 尚未跟进该版本），**必须明确告知用户无法为该版本编写插件**，除非用户明确说明本地有「已跟进该 Terraria 最新版本的 TShock 源码」。目标为 1.4.5.7/1.4.5.8 等新版本时，先提示实验性风险（OTAPI/TShock 可能未完整适配）。详见 `references/02-版本解析与兼容性.md`。

其余信息（插件名/作者/功能/命令权限/是否有本地测试服务器/是否有 .NET SDK）按问卷采集。小白答不上来的项留空，进入全自动准备路径，不阻塞。

**需求变更（任何阶段适用）**：开发中用户提出新增/修改需求 → 先更新需求摘要 → 说明本次变更影响哪些阶段并明确重跑路径（改配置项 → Phase 3/4；改命令/逻辑 → Phase 4/6；改目标版本 → Phase 1）→ 用户确认后从对应阶段继续。禁止不更新摘要直接改代码。

### Phase 1 环境与版本探测（动态解析）

按 `references/01-环境检测与自动准备.md` 与 `references/02-版本解析与兼容性.md` 执行：

1. `dotnet --version` 探测 SDK；缺失则提示安装（征得同意后自动安装）
2. 定位服务器目录：用户提供 > 常见位置探测 > 自动下载官方发布包（征得同意）
3. **动态解析版本四元组**：客户端版本 ↔ 服务端版本 ↔ TShock 版本 ↔ .NET TFM。每轮开发必做，不缓存旧结论
4. 触发版本门禁条件时（见 Phase 0）按门禁处理

### Phase 2 参考源码获取（分层）

按 `references/03-参考源码获取.md` 四层策略：

- **L1 本地版本化源码夹**（用户提供：TShock源码 + OTAPI + Terraria反编译）→ 存在则读取，作为 API 行为的最权威依据
- **L2 本地插件收集仓库** → 按需求搜索相似插件源码作参考
- **L3 NuGet 包**（官方 `TShock` 包，编译引用基础，必需）
- **L4 自动下载**（无本地源码时默认执行，先征得同意）：TShock 官方发布 zip（测试服务器）+ TShock 源码（tag）+ **TerrariaServerAPI 子模块源码**（`.gitmodules` 里找，定义 `TerrariaApi.Server` 类型如 `PacketTypes`）+ **OTAPI 源码**（版本见 TShock 发布说明）+ 社区插件库参考

结束时向用户输出「本次读取了哪些参考文件」清单。

### Phase 3 项目脚手架

按 `references/04-项目脚手架.md` 与 `references/05-代码组织规范.md`，用 `templates/` 生成：

- csproj（TFM 与 TShock 包版本由 Phase 1 解析结果确定）
- 分类封装的项目结构：命令/配置/缓存/工具/业务逻辑各归其类
- 解决方案 sln、测试项目（若存在可测逻辑）

### Phase 4 分层 TDD 实现

按 `references/07-分层TDD.md`：

- **纯逻辑**（不触碰 `Main.*`/`TShock.*` 运行时静态状态的类）→ 建 xunit 测试项目，**先写失败测试再实现**（Red-Green-Refactor）
- **粘合层**（钩子/命令/事件，依赖游戏运行时）→ 编译 + 服务器加载验证
- 全程中文 `//` 简明注释 + `#region 分区`

### Phase 5 代码审查

按 `references/08-代码审查清单.md` 逐项自查并修复：事件注册/注销配对、线程安全、权限检查、JSON 异常处理、API 版本兼容、命名与注释、单测覆盖。向用户报告审查结论。

### Phase 6 编译验证

- `dotnet build`：无错误无警告
- 有测试项目则 `dotnet test`：全绿

### Phase 7 部署与加载验证

按 `references/09-编译部署加载验证.md`：

- 复制 DLL 到服务器 `ServerPlugins/`
- 重启服务器（先 taskkill，再启动），检查 ServerLog/console：插件加载无报错、服务器不崩溃
- 失败 → 按错误类型回退到对应阶段，修复后从该阶段重新推进：
  - 编译错误/警告 → Phase 6 编译验证
  - 加载失败/启动崩溃 → Phase 5 代码审查 → Phase 6
  - 行为与需求不符 → Phase 4 分层 TDD
  - 回退矩阵详见 `references/09-编译部署加载验证.md`

### Phase 8 终审代码审查（交付前末道关卡）

按 `references/11-终审代码审查.md` 对本次开发/修改的**全部代码**做整体终审，重点确认四件事：

1. **方法名是否正确**：动词短语、无拼写错误、语义准确、`Is/Has/Can` 与 `Async` 后缀等命名惯例、无重复方法
2. **是否有缺陷**：空引用/越界/死循环/资源泄漏/线程安全/权限与登录绕过（高危），边界条件/异常吞噬/魔法数字/竞态/热路径性能（中危），冗余分支/不可达代码/误导注释（低危）
3. **是否有多余字段**：未引用的字段与配置项、重复存储、不该公开的属性、恒 false 标志位、调试残留、多余 using
4. **代码整体上有没有问题**：类职责单一 ≤400 行、无重复代码、注释与实现一致、分层清晰、API 与参考源核对、事件注册注销配对
5. **需求覆盖核对**：逐条对照 Phase 0 需求摘要（功能/命令/权限/配置/边界条件），确认全部实现、无遗漏无偏离

审查发现的问题**必须全部修复**后才可进入交付；修复后需重新编译（Phase 6）并重新部署验证（Phase 7），再复审直到通过。向用户报告审查结论（格式见 `references/11-终审代码审查.md`）。

### Phase 9 交付

- 输出：插件 DLL、README（命令表/权限表/配置说明）、单测报告、审查结论、本次使用的版本四元组
- 明确告知用户：剩余功能测试由用户自行完成

## 硬性规则

1. **版本门禁**：目标版本无对应 NuGet 包且用户无跟进源码 → 停止并说明，绝不硬编
2. **下载需同意**：任何联网下载/安装动作前先向用户说明并征得同意
3. **分层 TDD**：纯逻辑必须先测试后实现；粘合层必须编译+加载验证
4. **分类封装**：一个类一个职责，每个类 ≤ 400 行；代码按类拆分，禁止所有方法堆在一个文件
5. **中文注释**：一律用 `//` 中文单行注释（类/方法/属性/字段都用行尾 `//` 或上一行 `//`），解释「为什么」而非「是什么」；**禁止 `/// <summary>` 等 XML 文档注释**
6. **不硬编码用户数据**：不写入用户个人路径/账号/服务器 IP；部署路径由用户提供或探测

## 参考文档索引

| 文件 | 用途 |
|---|---|
| `references/00-需求采集.md` | 必答问卷与需求引导 |
| `references/01-环境检测与自动准备.md` | 环境探测命令与自动准备流程 |
| `references/02-版本解析与兼容性.md` | 动态版本解析、门禁规则、兼容性 |
| `references/03-参考源码获取.md` | 四层参考源策略 |
| `references/04-项目脚手架.md` | csproj 生成规则、项目结构、命名 |
| `references/05-代码组织规范.md` | 分类封装规范（Plugin/Config/Cache/Utils/业务类） |
| `references/06-TShockAPI速查.md` | 核心 API 用法速查 |
| `references/07-分层TDD.md` | 单测策略与可测性设计 |
| `references/08-代码审查清单.md` | 审查自查项 |
| `references/09-编译部署加载验证.md` | 编译/部署/重启/日志验证流程 |
| `references/10-排错手册.md` | 常见错误与解决 |
| `references/11-终审代码审查.md` | 交付前终审清单（方法名/缺陷/多余字段/整体质量） |

## 模板使用说明

`templates/` 提供可直接改用的插件工程骨架（源自社区成熟模板规范）：

- `plugin.csproj.tpl` → 按解析结果替换 `{{TargetFramework}}`、`{{TShockVersion}}`
- `Plugin.cs.tpl` → 替换 `{{插件名}}`、`{{作者}}`、`{{版本}}`、`{{描述}}` 等占位符
- `Configuration.cs.tpl`、`CacheData.cs.tpl`、`Utils.cs.tpl`、`Commands.cs.tpl` → 按需求增删成员
- `README.md.tpl` → 交付时填写命令表/权限表/配置说明
- 用户提供自有模板时，优先遵循用户模板的规范，内置模板作兜底
